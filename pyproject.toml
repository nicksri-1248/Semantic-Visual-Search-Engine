[project]
name = "semantic-visual-search-engine"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Nikhil Srivastava", email = "nicksri1248@proton.me" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
semantic-visual-search-engine = "semantic_visual_search_engine:main"

[build-system]
requires = ["uv_build>=0.12.4,<0.13.0"]
build-backend = "uv_build"
